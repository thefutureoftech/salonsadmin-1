export interface UIStatus {

    rtlSet: boolean;

    loading: boolean;

    selectedMenuItem: string;

    initialLoading: boolean;

    return_url: string;

    showAddOrderFab: boolean;

    loadingText: string;

    showAppBar: boolean;

    loginPerformed: boolean;

    refreshed_url: string;

    close_detail_page_signal: boolean;

    spinLoader: boolean;

    loadingInChildComponent: boolean;
    
}